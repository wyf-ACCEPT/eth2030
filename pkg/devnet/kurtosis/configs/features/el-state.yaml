# EL State — Binary Tree, Validity-Only Partial State, Endgame State, Misc Purges
# Tests: binary trie operations, VOPS partial execution, state sustainability, legacy purging
# Verify: blocks produced, state root changes across blocks
participants:
  - el_type: geth
    el_image: eth2030:local
    cl_type: lighthouse
    cl_image: sigp/lighthouse:latest
    count: 2

network_params:
  network: kurtosis
  seconds_per_slot: 12
  genesis_delay: 10

additional_services:
  - dora
  - assertoor

assertoor_params:
  run_stability_check: true
