# CL Infrastructure — Beacon Specs, Tech Debt Reset, Distributed Builder, CL Proofs
# Tests: modernized beacon specs (consensus/unified_beacon_state.go), tech debt reset
#   (consensus/tech_debt_reset.go), distributed block building
#   (engine/distributed_builder.go), real-time CL proofs
#   (consensus/cl_proof_circuits.go, light/cl_proofs.go)
# Verify: blocks produced, Engine API responds, chain operational
participants:
  - el_type: geth
    el_image: eth2030:local
    cl_type: lighthouse
    cl_image: sigp/lighthouse:latest
    count: 2

network_params:
  network: kurtosis
  seconds_per_slot: 12
  genesis_delay: 10

additional_services:
  - dora
  - assertoor

assertoor_params:
  run_stability_check: true
