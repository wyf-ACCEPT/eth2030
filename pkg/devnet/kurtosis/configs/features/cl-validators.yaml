# CL Validators — Attester Stake Cap, 128K Attester Cap, APS, 1 ETH Includers
# Tests: attester stake cap (consensus/attester_cap.go), APS committee selection
#   (consensus/aps.go), 1 ETH includers (consensus/one_eth_includers.go)
# Verify: blocks produced, validator-related behavior via block headers
participants:
  - el_type: geth
    el_image: eth2030:local
    cl_type: lighthouse
    cl_image: sigp/lighthouse:latest
    count: 2

network_params:
  network: kurtosis
  seconds_per_slot: 12
  genesis_delay: 10

additional_services:
  - dora
  - assertoor

assertoor_params:
  run_stability_check: true
