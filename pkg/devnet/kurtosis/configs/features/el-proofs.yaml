# EL Proofs — Optional Proofs, Mandatory 3-of-5 Proofs, Proof Aggregation, AA Proofs
# Tests: optional proof generation, mandatory proof submission/verification, recursive aggregation, AA circuits
# Verify: chain progresses with proof infrastructure active, blocks produced
participants:
  - el_type: geth
    el_image: eth2030:local
    cl_type: lighthouse
    cl_image: sigp/lighthouse:latest
    count: 2

network_params:
  network: kurtosis
  seconds_per_slot: 12
  genesis_delay: 10

additional_services:
  - dora
  - assertoor

assertoor_params:
  run_stability_check: true
