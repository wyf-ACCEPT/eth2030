# Gas Repricing — Glamsterdam Conversion Repricing (18 EIPs)
# Tests: ecAdd, ecMul, ecPairing, blake2f, pointEval repriced gas
# Verify: precompile calls execute with correct gas, no OOG on repriced ops
participants:
  - el_type: geth
    el_image: eth2030:local
    cl_type: lighthouse
    cl_image: sigp/lighthouse:latest
    count: 2

network_params:
  network: kurtosis
  seconds_per_slot: 12
  genesis_delay: 20

additional_services:
  - dora
  - spamoor
  - assertoor

spamoor_params:
  spammers:
    - scenario: eoatx
      name: "Repricing Load"
      config:
        throughput: 300
        max_pending: 600
        max_wallets: 300

assertoor_params:
  run_stability_check: true
  run_transaction_test: true
  run_opcodes_transaction_test: true
