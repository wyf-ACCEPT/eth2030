# CL Security — 51% Attack Recovery, VDF Randomness, Secret Proposers
# Tests: attack auto-recovery (consensus/attack_recovery.go), VDF randomness
#   (crypto/vdf.go, consensus/vdf_consensus.go), secret proposers
#   (consensus/secret_proposer.go, consensus/vrf_election.go)
# Verify: chain progresses, blocks produced under security features
participants:
  - el_type: geth
    el_image: eth2030:local
    cl_type: lighthouse
    cl_image: sigp/lighthouse:latest
    count: 2

network_params:
  network: kurtosis
  seconds_per_slot: 12
  genesis_delay: 10

additional_services:
  - dora
  - assertoor

assertoor_params:
  run_stability_check: true
