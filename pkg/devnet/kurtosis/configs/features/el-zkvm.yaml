# EL zkVM — Canonical Guest, Canonical zkVM, STF in zkISA, zkISA Precompiles, Exposed zkISA
# Tests: RISC-V guest execution, proof backend, STF executor, zkISA bridge, precompile translation
# Verify: chain integrity, blocks produced with zkVM infrastructure active
participants:
  - el_type: geth
    el_image: eth2030:local
    cl_type: lighthouse
    cl_image: sigp/lighthouse:latest
    count: 2

network_params:
  network: kurtosis
  seconds_per_slot: 12
  genesis_delay: 10

additional_services:
  - dora
  - assertoor

assertoor_params:
  run_stability_check: true
