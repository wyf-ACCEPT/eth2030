# Shielded Transfers — Private L1 (BN254 Pedersen + ZK Circuits)
# Tests: Pedersen commitments, nullifier sets, range proofs, ZK circuits
# Verify: commitments valid, nullifiers prevent double-spend, proofs verify
participants:
  - el_type: geth
    el_image: eth2030:local
    cl_type: lighthouse
    cl_image: sigp/lighthouse:latest
    count: 2

network_params:
  network: kurtosis
  seconds_per_slot: 12
  genesis_delay: 20

additional_services:
  - dora
  - spamoor
  - assertoor

spamoor_params:
  spammers:
    - scenario: eoatx
      name: "Shielded Load"
      config:
        throughput: 100
        max_pending: 200
        max_wallets: 100

assertoor_params:
  run_stability_check: true
  run_transaction_test: true
