# Native Rollups — EXECUTE Precompile (EIP-8079)
# Tests: EXECUTE precompile calls, anchor contract state, rollup verification
# Verify: EXECUTE returns valid results, anchor state persists
participants:
  - el_type: geth
    el_image: eth2030:local
    cl_type: lighthouse
    cl_image: sigp/lighthouse:latest
    count: 2

network_params:
  network: kurtosis
  seconds_per_slot: 12
  genesis_delay: 20

additional_services:
  - dora
  - spamoor
  - assertoor

spamoor_params:
  spammers:
    - scenario: eoatx
      name: "Rollup Load"
      config:
        throughput: 200
        max_pending: 400
        max_wallets: 200

assertoor_params:
  run_stability_check: true
  run_transaction_test: true
  run_opcodes_transaction_test: true
